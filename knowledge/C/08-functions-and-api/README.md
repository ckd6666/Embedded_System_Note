# 08 — Functions and API

Functions as implementation units and as module/driver interfaces.

## Scope

- function declarations and definitions
- prototypes
- parameters
- pass-by-value semantics
- return values
- output parameters
- function pointers
- callbacks
- API contracts
- parameter validation
- type-safe interfaces
- driver and HAL interface patterns

Cross-translation-unit linkage belongs in `../02-declarations-types-and-lifetime/` and `../01-preprocessor-and-build/`.

## Look Here When

Designing or reading a driver API, callback, function pointer, parameter list, return value, or interface contract.

## Entries

No entries yet.
