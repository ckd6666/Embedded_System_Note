# 05 — Arrays, Strings, and Buffers

Contiguous data structures and length/bounds handling.

## Scope

- arrays
- multidimensional arrays
- array size
- array-to-pointer conversion at the interface boundary
- C strings
- null termination
- character buffers
- explicit buffer lengths
- bounds
- UART, SPI, CAN, DMA, and protocol buffers

Pointer mechanics belong in `../06-pointers-and-memory/`.

## Look Here When

An array, string, communication buffer, DMA buffer, length, or bounds issue is involved.

## Entries

No entries yet.
