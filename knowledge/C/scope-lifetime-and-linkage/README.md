# Scope, Lifetime, and Linkage

Where names are visible, how long objects exist, and how definitions connect across a program.

## Scope

- block, file, and function scope
- automatic and static storage duration
- object lifetime
- internal and external linkage
- `static`
- `extern`
- global and local objects
- dynamic storage and heap concepts

Build-time symbol resolution belongs in `../preprocessor-and-build/`.

## Look Here When

You need to understand `static`, `extern`, global visibility, object lifetime, or storage duration.

## Entries

No entries yet.
