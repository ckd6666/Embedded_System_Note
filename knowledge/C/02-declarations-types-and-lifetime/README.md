# 02 — Declarations, Types, and Lifetime

How C names and objects are declared, typed, made visible, stored, and connected across a program.

## Scope

- declarations and definitions
- fundamental types
- `typedef`
- `enum`
- type qualifiers such as `const`
- scope
- storage duration
- object lifetime
- internal and external linkage
- `static`
- `extern`
- initialization

Integer arithmetic rules belong in `../04-integers-and-bits/`. Exact object layout belongs in `../07-objects-and-data-layout/`.

## Look Here When

You need to understand what a declaration means, what type an object has, where a name is visible, how long an object exists, or what `static`/`extern` means.

## Entries

No entries yet.
